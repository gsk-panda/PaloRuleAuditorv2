version: '3.8'

services:
  panoruleauditor:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: panoruleauditor
    ports:
      - "3010:3010"
    environment:
      - NODE_ENV=production
      - PORT=3010
      - PANORAMA_URL=${PANORAMA_URL:-}
      - PANORAMA_API_KEY=${PANORAMA_API_KEY:-}
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:3010/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
